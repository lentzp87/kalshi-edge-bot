"""Weather model — REMOVED.

Weather temperature trading was paused after 80+ paper trades showed no
edge after spread + fees. Realized P&L diverged consistently from
predicted P&L across every edge bucket. The model file is intentionally
empty so any lingering imports fail fast instead of running stale code.

If you ever want to revive weather trading, restore from git history:
    git log -- src/models/weather.py
"""
